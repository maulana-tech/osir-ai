# Where the Django backend is. The dev server proxies Django paths there so the
# browser sees one origin; server components call it directly with your session cookie.
STUDIO_URL=http://localhost:8000
