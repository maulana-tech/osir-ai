# Server-side only. The console talks to Studio with one workspace-scoped API key;
# issue it from Organization → API Keys with: use_inbox, reply_from_inbox, create_posts,
# approve_posts, view_analytics, manage_workspace_settings.
STUDIO_URL=http://localhost:8000
STUDIO_API_KEY=bb_studio_...
